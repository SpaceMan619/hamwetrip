{
  "name": "hamwetrip-rules-tests",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Security rules tests for HamweTrip, run against the Firestore emulator.",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project demo-hamwetrip \"node --test\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "firebase": "^12.16.0"
  }
}
