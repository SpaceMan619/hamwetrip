# Features

Frontend features live here. Give each feature its own folder, for example
`voting/`, `ledger/`, `itinerary/`, or `vault/`. Keep a feature's screens,
widgets, state, and models together so parallel work stays isolated.
