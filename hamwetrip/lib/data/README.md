# Data layer

Put repository interfaces, API/Firebase implementations, and shared data
models here. The UI in `lib/features` should use these interfaces rather than
calling a backend service directly.
